<!DOCTYPE html>
<html>
  <head>
    <title>ImmersiveRailsAssessment</title>
    <%= csrf_meta_tags %>
    
    <link rel="stylesheet" type="text/css" href="http://fonts.googleapis.com/css?family=Rubik">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload'%>
  </head>

  <body>
  
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="/">Virtual Closet</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/closet">Closet</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/outfits">Outfits</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/profile">Profile</a>
          </li>
        </ul>
      </div>
    </nav>
    
    <div class="container-fluid">
      <% if flash %>
        <div class="alert-container">
          <% flash.each do |key, value| %>
            <% if key == "warning"  %>
              <div class="col-md-5 alert alert-<%= key %>">
                <h3 class="alert-heading"><mark>Failed!</mark></h3>
                <ul>
                <% value.each do |err|%>
                  <li><%= err %></li>
                
                <% end  %>
                </ul>
              </div>
            <% else  %>
            <div class="col-md-5 alert alert-<%= key %>"><%= value %></div>
            <% end %>
          <% end %>
        </div>
      <% end  %>  
      <%= yield %>
    </div>
  
  </body>